"use strict"; // Treat all the JS code as newer version

// alert( 3+3 ); // Can't use inside the nodejs, because it can be done inside the browser's engine only


let name = "Anil" // String datatype
let age = 22 // Number
let isLoggedIn = false // Boolean
let state;

// Primitive datatypes 

// number => 2 to the power 53
// bigint => large integers
// string = ""
// boolean = true/false
// null => object/standalone value which can be assigned to a variable
// undefined => value which defines a variable which is created but not initilized
// symbol => unique


// object

console.log(typeof "Anil") // string
console.log(typeof age) // number
console.log(typeof null) // object
console.log(typeof undefined) // undefined
